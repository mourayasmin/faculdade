#include <stdio.h> 
int main () {
int x, y, z;
char A, B, C, nx, ny, nz;
scanf("%d %d %d\n", &x, &y, &z);
scanf("%c%c%c", &nx, &ny, &nz); 
  // x - y - z ; nx == c , ny == b , nz == a
  
  
  // x - z - y
  
  // y - x - z 
  
  // y - z - x 
  
  // z - y - x 
  
  // z - x - y 







return 0; 
}
